name: GitHub
profileUrl: 'https://github.com/{identifier}'
verificationMethods:
  gist:
    type: http
    input:
      gistIt:
        type: string  
    url: 'https://api.github.com/gists/{input.gistId}'
    format: json
    path: '$.files.*.content'